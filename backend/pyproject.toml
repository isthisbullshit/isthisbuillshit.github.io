[project]
name = "isthisbullshit-backend"
version = "0.1.0"
description = "Cloudflare Python Worker for the isthisbullshit backend"
requires-python = ">=3.12"
dependencies = [
    "aiohttp",
    "fastapi[standard]",
    "pydantic",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
